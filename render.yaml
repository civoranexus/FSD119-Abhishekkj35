services:
  - type: static
    name: healthvillage-frontend
    root_dir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    env: static
    envVars:
      - key: VITE_API_URL
        sync: false
